# Specific experiments settings
# (default settings inherited from configs.experiment.ExperimentSettings)

defaults:
  - schema_experiment

_tags_:
  - CNN

resume_checkpoint: ~

datamodule:
  _target_: wind_forecast.datamodules.MultiChannelSpatialDataModule.MultiChannelSpatialDataModule

model:
  _target_: wind_forecast.models.CNNModel.CNNModel

system:
  _target_: wind_forecast.systems.Regressor.Regressor

cnn_ff_input_dim:
  - 29376
  - 1024

cnn_filters:
  - 32
  - 64

sequence_length: 1
